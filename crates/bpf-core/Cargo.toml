[package]
name = "bpf-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"
crate-type = ["cdylib"]

[target.'cfg(target_arch = "bpf")'.dependencies]
bpf-api = { path = "../bpf-api" }
