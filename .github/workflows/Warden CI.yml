name: Warden CI
on: [push, pull_request]
jobs:
  warden-scan:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./warden-ci
        with:
          mode: observe
